#!/bin/bash
# Ejecuta comandos Git en todos los repos de un workspace

# Detectar si el primer argumento es un workspace o un comando de Git
FIRST_ARG=$1

# Comandos comunes de Git
GIT_COMMANDS="status add commit push pull fetch checkout branch merge rebase log diff stash reset"

# Verificar si el primer argumento parece ser un comando de Git
IS_GIT_CMD=false
if [ -n "$FIRST_ARG" ]; then
    # Si empieza con -, es un flag de Git
    if [[ "$FIRST_ARG" == -* ]]; then
        IS_GIT_CMD=true
    else
        # Si es un comando común de Git
        for cmd in $GIT_COMMANDS; do
            if [ "$FIRST_ARG" = "$cmd" ]; then
                IS_GIT_CMD=true
                break
            fi
        done
    fi
fi

# Determinar workspace y argumentos de Git
if [ "$IS_GIT_CMD" = true ]; then
    # Primer argumento es Git, detectar workspace automáticamente
    WORKSPACE_PATTERN=""
    GIT_ARGS="$@"
else
    # Primer argumento es workspace
    WORKSPACE_PATTERN=$1
    shift
    GIT_ARGS="$@"
fi

# Detectar WORKSPACE_ROOT desde WS_TOOLS o por defecto
if [ -n "$WS_TOOLS" ]; then
    WORKSPACE_ROOT="${WS_TOOLS%/tools/workspace-tools}"
else
    # Fallback: asumir ubicación estándar
    WORKSPACE_ROOT=~/wrkspc.nubarchiva
fi
WORKSPACES_DIR=$WORKSPACE_ROOT/workspaces

# Detectar directorio del script (compatible bash/zsh)
if [ -n "$BASH_SOURCE" ]; then
    SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
else
    SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
fi

# Cargar funciones compartidas
source "$SCRIPT_DIR/ws-common.sh"
source "$SCRIPT_DIR/ws-colors.sh"

# Función de ayuda
show_help() {
    echo "Uso: ws git <nombre|patrón> <comando-git> [argumentos...]"
    echo ""
    echo "Ejecuta comandos Git en todos los repos de un workspace."
    echo ""
    echo "💡 Puedes usar coincidencia parcial para el nombre del workspace"
    echo ""
    echo "Ejemplos:"
    echo "  ws git nuba-8400 status"
    echo "  ws git master pull"
    echo "  ws git fac log --oneline -5"
    echo "  ws git 8089 fetch"
    echo ""
    echo "Comportamiento:"
    echo "  • Ejecuta 'git <args>' en cada repo del workspace"
    echo "  • Se detiene en el primer error"
    echo "  • Ignora directorios sin .git"
}

# Si no se especificó workspace, intentar detectar automáticamente
if [ -z "$WORKSPACE_PATTERN" ]; then
    WORKSPACE_PATTERN=$(detect_current_workspace)
    if [ -z "$WORKSPACE_PATTERN" ]; then
        error "❌ Error: no se especificó workspace y no se pudo detectar automáticamente"
        echo ""
        info "💡 Ejecuta este comando desde dentro de un workspace o especifica el nombre:"
        echo "   ws git <workspace> <comando-git>"
        echo ""
        show_help
        exit 1
    fi
    info "🔍 Workspace detectado: $WORKSPACE_PATTERN"
fi

if [ -z "$GIT_ARGS" ]; then
    error "❌ Error: debes especificar un comando para Git"
    echo ""
    show_help
    exit 1
fi

# Buscar workspace que coincida con el patrón
WORKSPACE_NAME=$(find_matching_workspace "$WORKSPACE_PATTERN" "$WORKSPACES_DIR")
if [ $? -ne 0 ]; then
    exit 1
fi

# Determinar directorio del workspace
WORKSPACE_DIR=$WORKSPACES_DIR/$WORKSPACE_NAME
BRANCH_NAME=$(get_branch_name "$WORKSPACE_NAME")

if [ ! -d "$WORKSPACE_DIR" ]; then
    error "❌ Workspace no existe: $WORKSPACE_NAME"
    exit 1
fi

print_header "Ejecutando Git en workspace"
echo ""
echo "Workspace: ${COLOR_CYAN}$WORKSPACE_NAME${COLOR_RESET}"
echo "Branch:    ${COLOR_CYAN}$BRANCH_NAME${COLOR_RESET}"
echo "Comando:   ${COLOR_BOLD}git $GIT_ARGS${COLOR_RESET}"
echo ""

# Encontrar todos los repos en el workspace
repos=$(find_repos_in_workspace "$WORKSPACE_DIR")

if [ -z "$repos" ] || [ "$repos" = "" ]; then
    error "❌ No hay repos en este workspace"
    exit 1
fi

# Ejecutar Git en cada repo
echo "$repos" | while read -r repo_rel_path; do
    if [ -n "$repo_rel_path" ]; then
        repo_path="$WORKSPACE_DIR/$repo_rel_path"

        if [ -d "$repo_path/.git" ] || [ -f "$repo_path/.git" ]; then
            print_separator_with_title "${COLOR_CYAN}$repo_rel_path${COLOR_RESET}"
            echo ""

            # Cambiar al directorio del repo y ejecutar Git
            cd "$repo_path"
            git $GIT_ARGS

            # Verificar el resultado
            exit_code=$?
            if [ $exit_code -ne 0 ]; then
                echo ""
                error "❌ Error en $repo_rel_path (exit code: $exit_code)"
                error "   Deteniendo ejecución"
                exit $exit_code
            fi

            echo ""
        fi
    fi
done

# Capturar el exit code del while loop
exit_code=$?

if [ $exit_code -ne 0 ]; then
    exit $exit_code
fi

success "✅ Git ejecutado correctamente en todos los repos"
