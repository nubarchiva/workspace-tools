#!/bin/bash
# Cambia al workspace especificado y muestra su información

WORKSPACE_PATTERN=$1

# Detectar WORKSPACE_ROOT desde WS_TOOLS o por defecto
if [ -n "$WS_TOOLS" ]; then
    WORKSPACE_ROOT="${WS_TOOLS%/tools/workspace-tools}"
else
    # Fallback: asumir ubicación estándar
    WORKSPACE_ROOT=~/wrkspc.nubarchiva
fi
WORKSPACES_DIR=$WORKSPACE_ROOT/workspaces
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Cargar funciones compartidas
source "$SCRIPT_DIR/ws-common.sh"

# Función de ayuda
show_help() {
    echo "Uso: ws switch [nombre|patrón]"
    echo ""
    echo "💡 Puedes usar coincidencia parcial para el nombre del workspace"
    echo ""
    echo "Ejemplos:"
    echo "  ws switch                    # lista workspaces disponibles"
    echo "  ws switch master"
    echo "  ws switch nuba-8400"
    echo "  ws switch fac                # busca 'fac' en workspaces"
}

# Si no hay argumentos, listar workspaces
if [ -z "$WORKSPACE_PATTERN" ]; then
    echo "Workspaces disponibles:"
    echo ""

    if [ -d "$WORKSPACES_DIR" ]; then
        for workspace_dir in "$WORKSPACES_DIR"/*; do
            if [ -d "$workspace_dir" ]; then
                workspace_name=$(basename "$workspace_dir")
                branch_name=$(get_branch_name "$workspace_name")
                echo "  • $workspace_name (branch: $branch_name)"
            fi
        done
    fi

    echo ""
    show_help
    exit 0
fi

# Buscar workspace que coincida con el patrón
WORKSPACE_NAME=$(find_matching_workspace "$WORKSPACE_PATTERN" "$WORKSPACES_DIR")
if [ $? -ne 0 ]; then
    exit 1
fi

# Determinar directorio del workspace
WORKSPACE_DIR=$WORKSPACES_DIR/$WORKSPACE_NAME
BRANCH_NAME=$(get_branch_name "$WORKSPACE_NAME")

if [ ! -d "$WORKSPACE_DIR" ]; then
    echo "❌ Workspace no existe: $WORKSPACE_NAME"
    echo ""
    echo "Workspaces disponibles:"
    $0
    exit 1
fi

# Mostrar información del workspace
echo "╔════════════════════════════════════════════════════╗"
echo "║  Workspace: $WORKSPACE_NAME"
echo "╚════════════════════════════════════════════════════╝"
echo ""

# Mostrar README si existe
if [ -f "$WORKSPACE_DIR/README.md" ]; then
    cat "$WORKSPACE_DIR/README.md"
    echo ""
    echo "═══════════════════════════════════════════════════"
    echo ""
fi

# Mostrar estado de cada repo (incluyendo subdirectorios)
echo "Estado de los repos:"
echo ""

repos=$(find_repos_in_workspace "$WORKSPACE_DIR")
has_repos=false

echo "$repos" | while read -r repo_rel_path; do
    if [ -n "$repo_rel_path" ]; then
        has_repos=true
        worktree_path="$WORKSPACE_DIR/$repo_rel_path"

        if [ -d "$worktree_path" ]; then
            echo "📦 $repo_rel_path:"
            cd "$worktree_path"

            # Branch actual
            current_branch=$(git branch --show-current)
            echo "   Branch: $current_branch"

            # Estado
            status=$(git status -s)
            if [ -z "$status" ]; then
                echo "   ✅ Sin cambios"
            else
                echo "   ⚠️  Cambios sin commitear:"
                git status -s | sed 's/^/      /'
            fi
            echo ""
        fi
    fi
done

if [ -z "$repos" ] || [ "$repos" = "" ]; then
    echo "  (No hay repos en este workspace)"
    echo "  💡 Usa 'ws add $WORKSPACE_NAME <repo>' para añadir"
    echo ""
fi

echo "═══════════════════════════════════════════════════"
echo ""
echo "📁 Ruta: $WORKSPACE_DIR"
echo ""
echo "Para trabajar aquí:"
echo "  cd $WORKSPACE_DIR"
echo ""

if [ -n "$repos" ]; then
    echo "O con un repo específico:"
    echo "$repos" | while read -r repo_rel_path; do
        if [ -n "$repo_rel_path" ]; then
            echo "  cd $WORKSPACE_DIR/$repo_rel_path"
        fi
    done
fi
